<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
                                 https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <!-- Production: file-based H2 database stored in ./uno-history.mv.db -->
    <persistence-unit name="uno-pu" transaction-type="RESOURCE_LOCAL">
        <class>GameSessionEntity</class>
        <class>RoundEntity</class>
        <class>PlayerScoreEntity</class>
        <exclude-unlisted-classes>true</exclude-unlisted-classes>
        <properties>
            <property name="jakarta.persistence.jdbc.driver"   value="org.h2.Driver"/>
            <property name="jakarta.persistence.jdbc.url"      value="jdbc:h2:./uno-history"/>
            <property name="jakarta.persistence.jdbc.user"     value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>
            <!-- create tables on first run, keep them on subsequent runs -->
            <property name="hibernate.hbm2ddl.auto"            value="update"/>
            <property name="hibernate.show_sql"                value="false"/>
        </properties>
    </persistence-unit>

    <!-- Test: in-memory H2 database, schema recreated on every test run -->
    <persistence-unit name="uno-test-pu" transaction-type="RESOURCE_LOCAL">
        <class>GameSessionEntity</class>
        <class>RoundEntity</class>
        <class>PlayerScoreEntity</class>
        <exclude-unlisted-classes>true</exclude-unlisted-classes>
        <properties>
            <property name="jakarta.persistence.jdbc.driver"   value="org.h2.Driver"/>
            <property name="jakarta.persistence.jdbc.url"      value="jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1"/>
            <property name="jakarta.persistence.jdbc.user"     value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>
            <!-- drop and recreate schema on each test run for full isolation -->
            <property name="hibernate.hbm2ddl.auto"            value="create-drop"/>
            <property name="hibernate.show_sql"                value="false"/>
        </properties>
    </persistence-unit>

</persistence>

